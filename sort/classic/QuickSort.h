#pragma once

#include <string>
#include <vector>

// ------------------------- quickSort -------------------------
size_t quickSort(std::vector<std::string>::iterator begin, std::vector<std::string>::iterator end);
